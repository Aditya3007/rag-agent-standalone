# Minimal dependencies for agent service (when using remote retrieval API)
pyyaml>=6.0.1
groq>=0.9.0
openai>=1.30.0
python-dotenv>=1.0.1
requests>=2.31.0

# For full local development with all ML dependencies:
# pip install -e ".[full]"

# For testing:
# pip install pytest
