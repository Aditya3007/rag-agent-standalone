# Minimal dependencies for Vercel agent service (when using remote retrieval API)
# Core RAG framework (minimal subset)
pyyaml>=6.0.1

# LLM providers
groq>=0.9.0
openai>=1.30.0

# Config / env
python-dotenv>=1.0.1

# API client for remote retrieval
requests>=2.31.0

# FastAPI for local testing (optional)
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
